
- finish demos - draggable, edge snapping
- put on github
- history management -> what happens when user goes "back" from first page in layer? Should we make it close layer? Optional plugin?
    - note - would probably work well, but ONLY if top-most frame is "back-outable". It it pushed an alert style, and then user went back, would not back out
    - basically -> it only makes sense if it's global




push_state(instructions for opening modal)